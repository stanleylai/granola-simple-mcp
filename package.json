{
  "name": "granola-simple-mcp",
  "version": "1.0.0",
  "description": "MCP server for Granola meeting notes — list, search, and retrieve notes and transcripts via the official Granola API",
  "bin": {
    "granola-simple-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && echo '#!/usr/bin/env node' | cat - dist/index.js > dist/index.tmp && mv dist/index.tmp dist/index.js && chmod +x dist/index.js",
    "prepublishOnly": "npm run build",
    "start": "tsx src/index.ts"
  },
  "keywords": [
    "mcp",
    "granola",
    "meeting-notes",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
